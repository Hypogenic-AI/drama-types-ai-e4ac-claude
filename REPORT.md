# Different Kinds of Drama — Research Report

**How do LLMs represent what different people find dramatic? A geometry-of-representations study.**

Models: `Qwen/Qwen2.5-7B-Instruct` (primary), `Qwen/Qwen2.5-1.5B-Instruct` (scale
ablation). Stimulus generation/validation: a 28-agent workflow (Claude). Date: 2026-06-22.

---

## 1. Executive Summary

We asked whether an LLM represents the dramatic-vs-melodramatic distinction — and
the way *different readers* draw it — as **thresholds on a single axis** (H1: a
low-dimensional drama-feature space) or as **reader-specific directions within a
drama subspace** (H2: a reader-feature space containing drama subspaces). We
separate two questions that the literature usually conflates: the geometry of the
**drama construct** and the geometry of **reader variation**.

We find a **split answer**, and it is the interesting part:

1. **The drama construct is genuinely multi-dimensional, not one axis.** "Drama"
   is perfectly linearly decodable (AUROC 1.00), but **melodrama is not "just more
   drama"**: the dramatic→melodramatic step rotates to **~80–86° (nearly
   orthogonal)** to the drama axis in deep layers, and a subspace built from 8
   drama subconcepts has a **participation ratio rising to ~5.6–5.9** (vs ~8 for
   random directions). Melodrama lives on a distinct **"excess" direction**. So
   "different *kinds* of drama" are real, separate directions — **H1's
   single-axis-with-thresholds picture is false for the construct.**

2. **Reader variation, by contrast, is overwhelmingly a threshold on a shared
   low-dimensional axis — H1.** Using 70 identified human raters in GoEmotions
   (real disagreement on what is dramatic), a per-reader **threshold** on one
   shared drama axis lifts held-out AUROC from 0.51→0.62 (transferred subspace)
   and 0.66→0.70 (in-domain). But letting each reader choose their **own direction**
   inside a 30-dimensional drama subspace adds **exactly the shuffled-reader null**
   (ΔAUROC = +0.0076 real vs +0.0076 null, z≈1.0, n.s.). Prompting six reader
   **personas** leaves the drama axis essentially fixed (pairwise cosine ≥ 0.97).

**One-sentence finding:** drama is multi-dimensional and *shared* across readers,
but **people mostly differ by where they put the threshold, not by which direction
they read** — "different kinds of drama" are in the content, while "different
people" are (mostly) a sliding threshold.

---

## 2. Research Question & Motivation

LLMs increasingly make affect-laden subjective judgments (moderation, storytelling,
companionship, criticism). If a model collapsed "drama" onto one intensity axis it
would conflate *earned drama* with *melodramatic excess* and mis-model readers who
differ in **kind**, not just **degree**. The user's question, transposed onto
representation geometry, is the same single-direction-vs-subspace debate that is
unsettled even for well-studied concepts (refusal: Arditi 2024 "one direction" vs
Wollschläger 2025 "concept cone"). No prior work studies "drama" or the
dramatic-vs-melodramatic contrast, nor joins **model-internal affect geometry**
with **per-reader behavioral variation**. We do both.

**Hypotheses.** H1 — drama is a low-D feature space; readers differ by
thresholds/regions (single-axis limit: one drama direction + per-reader threshold).
H2 — reader variation is a *direction within* a multi-D drama subspace
(reader⊗drama factorization).

---

## 3. Experimental Setup

### Models & extraction
- **Qwen2.5-7B-Instruct** (28 layers, hidden 3584) and **Qwen2.5-1.5B-Instruct**
  (28 layers, hidden 1536), HuggingFace, **bfloat16** on one RTX A6000
  (fp16 produced NaNs — documented). Hidden states **mean-pooled over text tokens**
  at layers spanning depth {0.25…1.0}. Raw text (no chat template) for the base
  geometry; chat template with a system persona for the persona probe.
- All geometry uses **difference-in-means directions** (robust & causal; Marks &
  Tegmark 2023). Seeds fixed at 42 throughout.

### Stimuli & data
- **Controlled drama corpus (built here):** 24 base scenarios × 4 registers
  {neutral, understated, dramatic, melodramatic} = 96 register texts, plus 8 drama
  subconcepts (suspense, pathos/grief, spectacle, stakes, sincerity,
  excess/sentimentality, shock, restraint) × ~24 contrast texts = 194 texts.
  Generated by a **28-agent workflow** and **adversarially blind re-labeled** by
  independent agents; only label-agreed items kept (**290/290 passed**, 100%
  agreement; perfectly register-balanced). `data/corpus_*.parquet`.
- **GoEmotions (raw):** 211k annotations, **70 identified raters** (after keeping
  texts with ≥3 raters and raters with ≥150 labels), **56,995 texts**. "Drama" =
  perceived emotional **intensity/arousal**: per (text,rater) `drama_label`=1 iff
  the rater chose any high-arousal emotion (arousal ≥ 0.70 on a circumplex/NRC-VAD
  mapping). Base rate 0.185; **36% of texts are "contested"** (rater drama-rate in
  [0.15,0.85]) — substantial real disagreement. `data/ge_*.parquet`.
- **EmoBank** inspected as auxiliary (its individual ratings are anonymized — no
  rater identity across items — so used only at the aggregate level; not central).

### Metrics
Probe AUROC; **participation ratio / principal angles** (subspace dimensionality);
held-out (text-disjoint) AUROC for nested models with **bootstrap 95% CIs** and a
**shuffled-reader permutation null**; persona-axis cosine; reader-weight-matrix rank.

### Cost
APIs used only for stimulus generation/validation (the 28-agent workflow); all
model-internal work ran locally on GPU. No paid LLM-API tokens were spent on the
core experiments.

---

## 4. Results

### E1 — The drama construct: is melodrama "just more drama"? **No.**

| Layer (7B) | drama-axis AUROC | cos(drama, melo axis) | angle of dramatic→melodramatic step vs drama axis | subspace PR (of 8) |
|---|---|---|---|---|
| 16 (0.57) | 1.000 | 0.999 | 56° | 1.63 |
| 21 (0.75) | 1.000 | 0.998 | 79° | 2.18 |
| 28 (1.0)  | 1.000 | 0.830 | **83°** | **5.64** |

The dramatic→melodramatic displacement becomes **nearly orthogonal** to the drama
axis in deep layers (replicated in 1.5B: 25°→**85°**). Melodrama separates cleanly
on a distinct **"excess" axis** (see `figures/drama_excess_plane.png`: dramatic and
melodramatic sit at the *same* drama-axis value but split on the orthogonal excess
axis). **Melodrama is a different direction, not a higher threshold.**

### E2 — Drama subspace is low-rank but multi-dimensional.

Participation ratio of the 8 subconcept directions rises with depth to **~5.6 (7B)
/ ~5.9 (1.5B)**, far below the **~8.0** of random directions but well above **1**.
The subconcept cosine structure is interpretable (e.g. *shock*↔*suspense* correlate;
*pathos/grief*↔*restraint* anti-correlate). So drama is a **genuine low-dimensional
*space* of features**, not a single axis — consistent with H1's *low-dimensional*
premise but refuting its *single-axis* limit. (`figures/e1_e2_geometry.png`)

### E3 — Reader variation (the crux): threshold, not direction. **H1.**

Nested logistic models predict per-rater `drama_label` from text activations;
held-out on **disjoint texts**; 70 readers.

**Transferred corpus subspace (layer 21):**

| Model | what it adds | held-out AUROC |
|---|---|---|
| M0 shared single axis | — | 0.510 |
| M1 + per-reader **threshold** | reader intercept | **0.619** |
| M2 + per-reader gain | reader slope (1-axis) | 0.622 |
| M3 + per-reader **direction** in 9-D subspace | reader weight vector | 0.623 |

ΔM3−M2 = +0.0006 = **shuffled-reader null** (+0.0006), z = −0.5.

**In-domain PCA subspace (K=30), the fair test:**

| Model | layer 21 | layer 28 |
|---|---|---|
| M0 shared 1 axis | 0.660 | 0.641 |
| M1 + reader **threshold** | **0.700** | **0.686** |
| M2K shared **K-dim** + threshold | 0.701 | 0.687 |
| M3 reader-specific **K-dim direction** | 0.708 | 0.691 |
| **ΔM3−M2K real vs shuffled-null** | +0.0076 vs **+0.0076** (z=1.0) | +0.0046 vs **+0.0046** (z=−0.2) |

Two facts dominate, both replicated across layers, K∈{10,30}, and the transferred
subspace:
- The decisive lift is **M0→M1 (per-reader threshold)**: +0.04 to +0.11 AUROC.
- The shared full subspace barely helps beyond one axis (M2K−M1 ≈ +0.001), and
  **reader-specific directions add nothing beyond the shuffled-reader null**
  (M3−M2K real ≡ null). The apparent M3 gain is pure model-capacity, present even
  when reader identity is randomized. (`figures/e3b_indomain.png`)

**Cross-model replication (1.5B, layer 21):** M0=0.661 → **M1=0.702** (threshold
jump) → M2K=0.703 → M3=0.711, with real ΔM3−M2K=+0.0084 = **null +0.0084** (z=0.28).
Identical pattern at a different scale.

So real readers' drama judgments are well-modeled as a **threshold on a shared,
low-dimensional drama axis** — H1, not H2.

### E4 — Persona probe: prompting a reader does not rotate the drama axis.

Re-deriving the drama axis under six reader personas (cynic, romantic, stoic,
thrill-seeker, critic, neutral), the pairwise cosine between persona-conditioned
drama axes is **0.99 mean (≥0.974 min)** — essentially the **same direction**.
Personas shift the *projection/threshold*, not the *axis*. Representational evidence
agrees with the behavioral H1 verdict. (`figures/e4_persona.png`)

---

## 5. Analysis & Discussion

**A clean dissociation.** The construct of drama is multi-dimensional and *shared*;
reader variation rides it as a near-1-D threshold. "Different kinds of drama"
(dramatic vs melodramatic, suspense vs pathos vs spectacle) are real, separable
**content directions** in the model. "Different people," however, are mostly a
**sliding threshold** on the same shared geometry — at least for the kind of
disagreement captured by GoEmotions intensity labels and by prompted personas.

**Why this is not trivial.** A priori the user's hypothesis (reader X's "dramatic" =
reader Y's "melodramatic", not modeled as thresholds on one axis) could have been
true two ways: (a) melodrama is off-axis from drama — **confirmed** (E1/E2); and
(b) readers read drama off *different directions* — **not supported** (E3/E4). The
second is the stronger H2 claim, and it fails the shuffled-reader control: the
multidimensionality of drama is in the stimulus representation, available to all
readers equally, and individuals select an intensity threshold on it rather than a
personal direction.

**Relation to literature.** Consistent with low-dimensional, steerable affect
manifolds (Reichman 2025) and with sentiment-as-one-direction (Tigges 2023) for the
*shared* axis; consistent with Orlikowski (2025) that reader effects are real and
learnable (here: large per-reader thresholds) but **not** low-D demographic
structure — and we add that they are also **not a reader-specific drama direction**.
The melodrama-as-excess axis is a drama-specific instance of the
concept-cone/subspace findings (Wollschläger 2025): even an "affective" construct
needs >1 dimension, just not a *reader-conditioned* one.

**Error/edge analysis.** Overall AUROC ceilings (~0.62–0.71) reflect proxy-label
noise (intensity≠literal drama) and irreducible annotator stochasticity; the *shape*
of the nested ladder (huge M1 jump, flat M2K/M3) is stable regardless of the ceiling.
The transferred-subspace and in-domain analyses agree, so the verdict is not an
artifact of how the subspace is defined.

---

## 6. Limitations

- **"Drama" is operationalized as arousal/intensity** for GoEmotions (an emotion
  benchmark, not a drama corpus); the controlled corpus uses LLM-authored register
  variants. Literal narrative drama may behave differently; the convergence of two
  operationalizations mitigates but does not eliminate this.
- **Reader labels are binary high-arousal indicators**; richer per-reader drama
  scales (or a purpose-built multi-reader drama-rating dataset) could expose
  reader-direction effects our binary label cannot resolve. The H1 verdict is "no
  *detectable* reader-specific direction," not proof of its absence.
- **One model family (Qwen2.5).** Geometry replicates across 1.5B/7B and layers,
  but cross-family generalization (Llama, Gemma) is untested.
- Activations are **mean-pooled**; token-level / SAE-feature analyses or causal
  steering (planned E4-causal, not run) could refine the picture.
- Stimuli are **Claude-authored**; though blind-validated, generator stylistic
  priors could shape the excess axis. The *subjects* of study (Qwen activations,
  human raters) are independent of the generator.

---

## 7. Conclusions & Next Steps

**Answer to the research question.** The model represents drama as a **low-dimensional
*space* of drama features (H1's premise), not a single axis** — melodrama is a
distinct, near-orthogonal "excess" direction, so "different kinds of drama" are
genuine. But it represents **reader variation as a threshold on a shared drama axis,
not as a reader-specific direction within a drama subspace** — i.e., the strong H2
("reader-feature space containing drama subspaces") is **not supported** for
naturally-occurring reader disagreement or for prompted personas. The drama
multidimensionality is in the **content**, shared across readers; the inter-reader
differences are (mostly) **where the threshold sits.**

**Next steps:** (1) build a purpose-made multi-reader *drama* rating set (graded,
not binary) to give H2 its best chance; (2) **causal steering** along the drama and
excess axes to confirm independence under intervention (Wollschläger-style);
(3) cross-family replication; (4) SAE/feature-circuit localization of the excess
direction; (5) test whether *fine-tuned* (not prompted) reader embeddings rotate the
axis where personas did not.

---

## References (used)
Reichman et al. 2025 *Emotions Where Art Thou*; Tigges et al. 2023 *Linear
Representations of Sentiment*; Marks & Tegmark 2023 *Geometry of Truth*; Arditi et
al. 2024 *Refusal…Single Direction*; Wollschläger et al. 2025 *Geometry of Refusal
(Concept Cones)*; Zhao et al. 2024 *Gaussian Concept Subspace*; Shah et al. 2025
*Geometry of Harmfulness*; Orlikowski et al. 2025 *Beyond Demographics*; Demszky et
al. 2020 *GoEmotions*; Buechel & Hahn 2017 *EmoBank*. Full set in
`literature_review.md` / `papers/`.

*Datasets:* GoEmotions (Apache-2.0), EmoBank (CC-BY-SA). *Tools:* PyTorch 2.12+cu130,
transformers 5.12, scikit-learn 1.9, numpy/scipy, matplotlib. Seeds = 42.
Raw results in `results/`, figures in `figures/`, code in `src/`.
